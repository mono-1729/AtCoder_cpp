#define ll long long
#include <bits/stdc++.h>
#include <stdlib.h>
using namespace std;
int main(){
    ll ng=-1,ok=1000000000000000000;
    while (abs(ok - ng) > 1){
        ll mid = (ok + ng) / 2 ;
        bool flg=false;

        if (flg) ok = mid;
        else ng = mid;
    }
}